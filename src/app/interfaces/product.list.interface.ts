// Estructura para la sección "Kit de Electrónica y Robótica"

export interface ProductListItem {
  id: number;
  label: string; // Nombre del producto (ej: SuperKit)
  imageUrl: string; // Ruta de la imagen
  price: string; // Precio formateado (ej: "$699.00")
}
